import 'package:flutter/material.dart';

class ChampionItem extends StatefulWidget {
  const ChampionItem({super.key});

  @override
  State<ChampionItem> createState() => _ChampionItemState();
}

class _ChampionItemState extends State<ChampionItem> {
  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.stretch,
        children: [],
      ),
    );
  }
}
